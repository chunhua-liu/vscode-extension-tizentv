export interface MappingResult {
    path: string;
    line: number;
    column: number;
}
export interface ISourceMaps {
    MapPathFromSource(path: string): string;
    MapFromSource(path: string, line: number, column: number): MappingResult;
    MapToSource(path: string, line: number, column: number): MappingResult;
    AllMappedSources(path: string): string[];
    /**
     * With a known sourceMapURL for a generated script, process create the SourceMap and cache for later
     */
    ProcessNewSourceMap(path: string, sourceMapURL: string): Promise<void>;
}
export declare class SourceMaps implements ISourceMaps {
    static TRACE: boolean;
    private static SOURCE_MAPPING_MATCHER;
    private _generatedToSourceMaps;
    private _sourceToGeneratedMaps;
    private _webRoot;
    constructor(webRoot: string);
    MapPathFromSource(pathToSource: string): string;
    MapFromSource(pathToSource: string, line: number, column: number): MappingResult;
    MapToSource(pathToGenerated: string, line: number, column: number): MappingResult;
    AllMappedSources(pathToGenerated: string): string[];
    ProcessNewSourceMap(pathToGenerated: string, sourceMapURL: string): Promise<void>;
    private _findSourceToGeneratedMapping(pathToSource);
    /**
     * pathToGenerated - an absolute local path or a URL.
     * mapPath - a path relative to pathToGenerated.
     */
    private _findGeneratedToSourceMapping(pathToGenerated, mapPath);
    private _createSourceMap(mapPath, pathToGenerated);
}
