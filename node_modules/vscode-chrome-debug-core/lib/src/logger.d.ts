export declare enum LogLevel {
    Log = 0,
    Error = 1,
}
export declare type ILogCallback = (msg: string, level: LogLevel) => void;
export declare function log(msg: string, level?: LogLevel, forceDiagnosticLogging?: boolean): void;
export declare function init(isServer: boolean, logCallback: ILogCallback): void;
/**
 * Enable diagnostic logging (for non-server mode).
 */
export declare function enableDiagnosticLogging(): void;
